-- let's write definition of sum first

-- sum[x] = x + sum[] = x + 0 = x PROVED

-- Better clean write

-- sum [x]
-- = {applying sum}
--     x + sum []
-- = {applying sum}
--     x + 0
-- = {applying +}
--     x